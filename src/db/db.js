//import json_data to user object
import db from './db.json';

export const profile = db.profile;

export const categories = db.categories;

export const instructors = db.instructors;

export const trainingClasses = db.training_classes;